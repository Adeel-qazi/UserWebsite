import { createContext } from "react";            //import createContext from react        understand context API

const noteContext = new createContext();          //this is syntax to create context  and holding the all state related to notes

export default  noteContext;   